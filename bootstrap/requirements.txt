# Memu Bootstrap Server Requirements
# Install with: pip install -r requirements.txt --break-system-packages

# Web framework
flask>=2.3.0

# HTTP requests (for health checks, Matrix API)
requests>=2.31.0

# QR Code generation
qrcode[pil]>=7.4.0

# Image processing (required by qrcode[pil])
Pillow>=10.0.0
